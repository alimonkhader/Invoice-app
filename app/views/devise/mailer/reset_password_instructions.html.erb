<p>Hello <%= @resource.name.presence || @resource.email %>,</p>
<p>Someone requested a password reset for your Nilam Invoice account.</p>
<p>You can reset your password using the link below:</p>
<p><%= link_to "Reset my password", edit_password_url(@resource, reset_password_token: @token) %></p>
<p>If you did not request this, you can ignore this email.</p>
